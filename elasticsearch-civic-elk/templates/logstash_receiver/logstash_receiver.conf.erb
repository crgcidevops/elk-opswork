input {
  udp {
     port => 5557
     buffer_size => 1452
     codec => collectd { }
     type => "collectd"
  }
  gelf {
    port => 5556
  }

}

output {
    #stdout {codec => rubydebug}
    kafka {
      topic_id => "logstash"
      codec => "json"
    }

}
